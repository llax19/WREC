# Model Artifacts

Local model weights are intentionally excluded from git.

The experiments referenced these local paths:

- `models/Mixtral-8x7B-Instruct-v0.1`
- `qwen1.5-MoE-A2.7B`

Restore compatible Hugging Face model snapshots to those paths, or adjust runner arguments to point at another local model directory.
